/**
 * SVG Mock for Jest Testing
 * This mock is used to handle SVG imports in Jest tests.
 * It returns a simple string representation instead of the actual SVG content.
 */
export default 'svg-mock';

// For named exports if needed
export const ReactComponent = 'svg-mock-component';
